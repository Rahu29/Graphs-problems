1) Create stack
2) Add vertex to it and set vertex to visited
3) Run while loop till stack is empty
4) pop head of queue and store it in variable called "current"
5) Print "current" variable
6) Get all adjecent elements and store them in stack one by one only if the element is not visited.